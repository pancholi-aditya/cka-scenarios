# User and RBAC Configuration

You are required to create a new Kubernetes user and configure RBAC
permissions for that user.

The user must authenticate using client certificates and be granted
specific permissions on Pods.
