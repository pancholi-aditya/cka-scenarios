# ImagePolicyWebhook Scanner Integration

You must integrate a container image scanner into a kubeadm-provisioned
Kubernetes cluster.

An incomplete admission controller configuration is available under
`/etc/kubernetes/bouncer`. A functional HTTPS scanner endpoint is available at
`https://smooth-yak.local/review`.
