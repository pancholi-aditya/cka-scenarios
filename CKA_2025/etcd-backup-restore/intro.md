# ETCD Backup and Restore

The Kubernetes cluster uses **etcd** as its backing data store.

You are required to take a backup of the etcd database and then
restore etcd from that backup.

This task must be performed using **etcdctl** and the existing
cluster certificates.
