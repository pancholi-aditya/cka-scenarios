# Ingress Configuration Using NGINX

An application is running in the cluster and is exposed internally
via a Kubernetes Service.

You are required to expose this Service externally using an
**NGINX Ingress** with host- and path-based routing.
